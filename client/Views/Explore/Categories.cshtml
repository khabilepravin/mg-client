
@{
    ViewData["Title"] = "Categories";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-inner">
    <h5>Explore</h5>
    <div class="list-group list-group-bordered mb-3">
        @foreach (var result in Model)
        {
            <a href="~/movies/popularquotes/@result["id"]/@result["name"]" class="list-group-item list-group-item-action">
                <div class="list-group-item-figure">
                    <div class="tile bg-success">
                        <span class="fas fa-film"></span>
                    </div>
                </div>
                <div class="list-group-item-body">@result["name"]</div>
            </a>
        }
    </div>
</div>